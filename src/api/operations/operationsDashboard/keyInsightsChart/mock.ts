import { ApiKeyInsightsChart } from './types';

export const mockResponse: ApiKeyInsightsChart = {
	insights: [
		'DT is overloaded between 9AM and 1PM',
		'Recurring low PF (inspect industrial customers',
		'No power cuts today',
		'98% collection efficiency',
	],
};
