/**
 * @enum
 */

export enum ButtonType {
  PRIMARY = "btn-primary",
  SECONDARY = "btn-secondary",
  DISABLED = "btn-disabled",
}

export enum HeadingType {
  MAIN = "text-2xl text-primary-blackMain font-semibold",
  SUB = "text-lg text-primary-blackMain font-semibold",
  SUBSUB = "text-base text-primary-blackMain font-semibold",
}

export enum TextInputType {
  NUMBER = "number",
  TEXT = "text",
  PASSWORD = "password",
  EMAIL = "email",
}