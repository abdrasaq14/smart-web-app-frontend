import { SitesDashboardFilters } from '../types';

export const MOCK_RESPONSE_SLEEP_TIME = 2000;
export const DEFAULT_DASHBOARD_FILTERS: SitesDashboardFilters = {
	search: '',
	sites: [],
	companies: [],
	start_date: null,
	end_date: null,
};
export const DEFAULT_REQUIRED_FIELD_ERROR_MESSAGE = 'This field is required';
export const SIDEBAR_WIDTH = '250px';

